# خطة إكمال ربط تطبيق النهضة (Flutter) بالـ Backend

> **مرجع العقد:** `../FLUTTER_API_DOCUMENTATION.md` (3502 سطرًا)
> **الخطة التفصيلية السابقة:** `INTEGRATION_PLAN.md` — هذا الملف لا يلغيها، بل
> يُصحّح صورة "ما المنجز فعلًا" بعد فحص الكود سطرًا سطرًا، ويرتّب ما تبقّى.
> **تاريخ الفحص:** 2026-09-19 (مُحدَّث) · `flutter analyze` → 63 مشكلة، **صفر أخطاء** (`withOpacity` مهجور + استيرادان غير مستخدمين + `use_build_context_synchronously` واحد في شاشة التعارض).

---

## 1. خلاصة تنفيذية — أين نحن بالضبط

التطبيق **ليس في منتصف الطريق كما يبدو من عدد الملفات**. الصورة الحقيقية:

| الطبقة | الحالة | الدليل من الكود |
|---|---|---|
| نواة الشبكة (Dio + envelope + 3 interceptors) | ✅ منجزة وجيدة | `core/network/` — `validateStatus < 500`، تفكيك الغلاف يدويًا، عميل refresh منفصل بلا حلقة لا نهائية |
| التخزين المحلي (Drift، 7 جداول) | ✅ منجزة | `storage/tables/sync_tables.dart` |
| طابور المزامنة (الكتابة فيه) | ✅ منجز | `sync/sync_queue.dart` — دمج أقسام + حارس `dedupId` |
| **محرّك المزامنة (التفريغ منه)** | ✅ **منجز 2026-09-19** | `sync/sync_engine.dart` — يفرّغ الطابور فعليًا الآن؛ مربوط بالإقلاع واستئناف التطبيق وعودة الاتصال. **يوجّه اليوم نوعي المفضّلة فقط** (الوحيدان اللذان لهما دالة `CasesApi` فعلية) — باقي الأنواع تبقى `pending` بأمان بانتظار المرحلة ٢ |
| المصادقة | ✅ منجزة | `auth_api.dart` + `auth_interceptor.dart` |
| القراءة (كاش أولًا) | ✅ منجزة | `cases_repository.dart` + `StreamProvider`s |
| **الكتابة (9 مسارات أقسام)** | ✅ **منجزة 2026-09-19** | `cases_api.dart` — 9 دوال PUT، موجّهة في `SyncEngine._dispatch` |
| **سير العمل (accept/reject/opinion)** | ✅ **منجز 2026-09-19** | `workflow_api.dart` — الثلاثة مسارات المتاحة لـ `social_worker` |
| الزيارات + المرفقات (طبقة البيانات) | ✅ منجزة | `field_visits_api.dart`، `attachment_uploader.dart` |
| **شاشة التفاصيل (10 تبويبات)** | ⚠️ **على Mock** | `case_details_providers.dart:8` → `MockCaseDetailsRepository()` |
| **البحث** | ⚠️ **على Mock** | `case_search_providers.dart` → `mock_case_search_repository` |
| **الإشعارات** | ⚠️ **ثابتة في الكود** | `notifications_screen.dart:25` → `_mockNotifications` |
| **FCM** | ❌ غير موجود | لا `firebase_core` ولا `firebase_messaging` في `pubspec.yaml` |

**التشخيص في جملة:** البنية التحتية ممتازة ومفهومة للعقد بعمق (الدمج، الـ dedup، أولويات التفريغ كلها صحيحة مقابل §14)، لكن **ثلاث قطع تربط كل شيء ببعضه غائبة**: محرّك المزامنة، مسارات الكتابة، وربط شاشة التفاصيل بالبيانات الحقيقية. بدونها التطبيق **يقرأ فقط** — الأخصائي يرى حالاته ولا يستطيع تعديل أي شيء.

> ⚠️ **الطابور يمتلئ ولا يُفرَّغ.** `toggleBookmark` تكتب في الطابور اليوم ولا شيء يقرأها. أي كتابة تُضاف قبل المرحلة ١ أدناه = بيانات تتراكم في جهاز المستخدم بلا مصير.

---

## 2. الـ Flow الكامل — كيف يعمل التطبيق (من العقد §16)

### 2.1 رحلة الأخصائي اليومية

```
تسجيل الدخول (X-Client-Type: mobile)
        │  الباك إند يرفض أي دور غير social_worker (§2.2)
        ▼
تسجيل توكن FCM ──────────► POST /notifications/device-tokens
        │                    (بعد كل دخول + كل استئناف للتطبيق §12)
        ▼
طابور العمل ─────────────► GET /dashboard/work-queue
        │                    (assigned / in_research / returned_to_worker)
        ▼
   ┌────┴────┐
   │ حالة جديدة مُسنَدة │
   └────┬────┘
        ▼
   قبول أو رفض ─────────► POST /cases/{id}/accept        [Idempotency-Key]
        │                  POST /cases/{id}/reject-assignment
        ▼  الحالة تصبح in_research
   ┌────┴─────────────────────────────┐
   │  نافذة البحث الميداني             │
   │  (in_research أو returned_to_worker) │
   └────┬─────────────────────────────┘
        ▼
   ملء 9 أقسام ─────────► PUT /cases/{id}/beneficiary     [rowVersion]
        │                  PUT /cases/{id}/family-members  [caseRowVersion]
        │                  PUT .../housing, utilities, agriculture,
        │                      financial, initial-needs,
        │                      classification, assessed-needs
        ▼
   الزيارة الميدانية:
        ├─ التقاط صور ──► POST /attachments/init
        │                  PUT  <uploadUrl>  (بايتات مباشرة للتخزين)
        │                  POST /attachments/{id}/commit   ← الكتابة الوحيدة
        │                                                     الآمنة بلا مفتاح
        ├─ الزيارة ─────► POST /cases/{id}/field-visits
        │                  ⚠️ لا Idempotency-Key ولا dedup server-side
        └─ التحقق ──────► PUT /cases/{id}/field-verification
                           (استبدال كامل، الخادم يحسب الفروق)
        ▼
   رأي الأخصائي ────────► POST /cases/{id}/opinions/worker [Idempotency-Key]
        ▼                  decision: accepted | rejected
   الحالة تصبح pending_review — خرجت من يد الأخصائي
        │
        ▼ (قد تعود)
   returned_to_worker ──► تعود لنافذة البحث من جديد
```

### 2.2 مسار الأوفلاين (§16.4) — ما يجب أن يفعله محرّك المزامنة

```
   أثناء انقطاع الشبكة:
   كل كتابة → Drift (تظهر فورًا للمستخدم) + صف في sync_queue
   الصور → تُنسَخ لمجلد دائم، ولا يُستدعى /init إطلاقًا
                                   (نافذة الـ 48 ساعة لا تُحرَق على اتصال غير موجود)
        │
        ▼ عودة الاتصال
   ١. التحقق من التوكن أولًا — قبل أي عنصر في الطابور (§14.4)
      refresh منتهٍ (طابور أقدم من 7 أيام) → إعادة دخول إجبارية
        ▼
   ٢. GET /cases/{id} — ما الوضع الحقيقي الآن؟ (الحالة قد تكون أُعيد إسنادها)
        ▼
   ٣. التفريغ بالترتيب الإلزامي لكل حالة على حدة:
      ٠ رفع المرفقات  →  ٢ قبول/رفض  →  ٣ الزيارات  →  ٤ الأقسام  →  ٥ الرأي
      (هذا الترتيب مُطبَّق بالفعل في flushPriority ✅)
        ▼
   ٤. معالجة الفشل — لكلٍّ مصير مختلف تمامًا:
      409 CONCURRENCY_CONFLICT → إعادة جلب + شاشة تعارض. لا دمج تلقائي.
      422 INVALID_STATUS_TRANSITION → توقّف عن تفريغ باقي طابور هذه الحالة
      403 FORBIDDEN → لم تعد مُسنَدة إليك. لا refresh (§15.9)
      خطأ شبكة → إعادة محاولة بتراجع أسّي، بنفس Idempotency-Key
```

---

## 3. الفجوات مرتّبة بالخطورة

### 🔴 حاجزة للإطلاق

| # | الفجوة | الأثر |
|---|---|---|
| **G1** | **`SyncEngine` غير موجود** | الطابور يمتلئ ولا يُفرَّغ أبدًا. لا شيء يصل للخادم |
| **G2** | **مسارات الكتابة (9 أقسام) غير موجودة** | الأخصائي لا يستطيع حفظ أي بيانات |
| **G3** | **سير العمل غير موجود** | لا قبول، لا رفض، لا رأي. الحالة عالقة في `assigned` للأبد |
| **G4** | **شاشة التفاصيل على Mock** | 10 تبويبات تعرض بيانات وهمية. أخطر فجوة على الثقة — تبدو تعمل وهي لا تعمل |
| **G5** | **شاشة التعارض غير موجودة** | أول 409 = عمل ميداني يوم كامل يضيع بلا مسار استرداد |

### 🟠 مهمة قبل الميدان

| # | الفجوة | الأثر |
|---|---|---|
| **G6** | البحث على Mock | `search/cases` جاهز في `cases_api.dart` ولم يُربَط |
| **G7** | الإشعارات ثابتة + لا FCM | لا يعرف الأخصائي بإسناد جديد إلا بفتح التطبيق |
| **G8** | لا شاشات لالتقاط الصور/الزيارة | الطبقة الخلفية جاهزة بالكامل والواجهة غائبة |
| **G9** | **`http://` لا `https://`** | رقم قومي ودخل وحالة صحية **نصًّا صريحًا على الشبكة**. موثّق في `app_config.dart` وما زال قائمًا |

### 🟡 ديون

`withOpacity` مهجور (58 موضعًا) · استيرادان غير مستخدمين · لا `WorkManager` للتفريغ في الخلفية · `background.png` بحجم 1.4 ميجا داخل المستودع.

---

## 4. خطة التنفيذ

### المرحلة ١ — محرّك المزامنة ✅ **منجزة 2026-09-19** 🔴 *(الأساس — كل ما بعده يعتمد عليه)*

**لماذا أولًا:** كل كتابة تُضاف قبله تتراكم بلا مصير.

- [x] `core/sync/sync_engine.dart` — الجهة الوحيدة التي تستدعي `*_api.dart`
- [x] التفريغ **لكل حالة على حدة** بترتيب `flushPriority` (مُطبَّق ✅)
- [x] التحقق من صلاحية `refresh token` **قبل** بدء التفريغ لا لكل عنصر (§14.4) — `AuthRepository.canFlushSyncQueue()`
- [x] **عملية `failed` أو `conflict` لا تُحذَف تلقائيًا أبدًا** (موروث من `SyncQueueDao`، والمحرّك لا يتجاوزه)
- [x] تشغيل عند: الإقلاع (`StartupService.run`)، استئناف التطبيق (`onResume`)، عودة الاتصال (`autoSyncOnReconnectProvider`)
- [x] قفل إعادة الدخول — `SyncEngine.isFlushing` يمنع تفريغين متوازيين؛ مُختبَر
- [x] نوع بلا مسار تنفيذ (المرحلة ٢ لم تُنجَز بعد) يبقى `pending` بلا أثر — **لا** `dead_lettered` ولا استهلاك محاولات؛ مُختبَر مع اختبار حلقة لا نهائية صريح
- [x] 9 اختبارات في `test/core/sync_engine_test.dart` — بوابة الجلسة، النجاح، عزل التعارض بين الحالات، القفل، الأنواع غير الموجَّهة
- [ ] `GET /cases/{id}` قبل تفريغ طابور الحالة — لالتقاط `rowVersion` الحقيقي **(يُنفَّذ مع المرحلة ٢** لأنه لا معنى له قبل وجود مسارات كتابة تستهلك `rowVersion` طازجًا فعليًا)
- [ ] تحديث `rowVersion` من **استجابة** كل PUT — **مع المرحلة ٢** لنفس السبب
- [ ] تصنيف الأخطاء لأنواع لم تُختبَر بعد (`422/403` لسير العمل تحديدًا) — الآلية العامة (`ApiErrorCode.isConflict`) موجودة ومُختبَرة في `sync_queue_test.dart`، يبقى فقط التأكد من تصنيف كل كود خطأ فعلي يرجعه كل مسار كتابة جديد عند إضافته

**ملاحظة معمارية:** المرفقات والزيارات الميدانية **لا** تمرّ عبر `SyncQueueDao` — لهما منطق تعافٍ خاص أعقد (`AttachmentUploader`, `FieldVisitsRepository.submit`) موجود ومُختبَر مسبقًا. `SyncEngine.flush()` يستدعيهما كخطوة منفصلة قبل تفريغ الطابور العام، فالترتيب الشامل (مرفقات ← زيارات ← أقسام ← رأي) يبقى صحيحًا دون تكرار منطقهما هنا.

### المرحلة ٢ — مسارات الكتابة ✅ **منجزة 2026-09-19** 🔴

- [x] 9 دوال PUT في `cases_api.dart` (`updateBeneficiary` … `updateAssessedNeeds`)
- [x] **تمييز `rowVersion` عن `caseRowVersion`** — موثّق بتعليق لكل دالة؛ الحمولة (payload) تُبنى في طبقة الاستدعاء (`enqueue`) لا هنا، فالتمييز الفعلي لا يزال مسؤولية المتصل عند تجهيز `payload`
- [x] `workflow_api.dart`: `accept` · `reject-assignment` · `opinions/worker` — الثلاثة فقط المتاحة لـ `social_worker` (الباقي يرجع 403 لهذا الدور فلم يُنفَّذ)
- [x] **المفتاح يُولَّد عند ضغط المستخدم** (مُطبَّق في `enqueue` ✅) **ويُعاد استخدامه حتى النجاح** — `SyncEngine._requireIdempotencyKey` يقرأه من صفّ الطابور المخزَّن، لا يولّد جديدًا أبدًا
- [x] `SyncEngine._isWired`/`_dispatch` وُسِّعا ليغطيا الـ 12 نوعًا الجديدة؛ 2 اختبار جديد + تحديث 3 اختبارات كانت تستخدم `updateHousing` كمثال "غير موجَّه" (استُبدلت بـ `updateSupportRecommendations`، الوحيد المتبقي بلا مسار)
- [ ] **المصيدة المالية:** تعديل `beneficiary`/`agriculture`/`family-members` يُعيد حساب الملخص ويزيد `rowVersion` الحالة — فيبطل `caseRowVersion` المخزّن لـ `/financial` **بصمت**. يُعاد الجلب قبل الحفظ المالي — **لم يُنفَّذ بعد**: `updateFinancial` اليوم يرسل الحمولة كما وصلته من الطابور بلا إعادة جلب استباقية؛ يحتاج منطقًا في طبقة الـ repository يتحقق من عمر الحمولة المخزّنة قبل هذا الاستدعاء تحديدًا
- [ ] المسح التلقائي في الزراعة — الواجهة تعكس **الاستجابة** لا مدخلات المستخدم — مسؤولية شاشة الزراعة في المرحلة ٣، لا طبقة الشبكة هنا

### المرحلة ٣ — ربط شاشة التفاصيل ✅ **منجزة جزئيًا 2026-09-19** 🔴

- [x] كل تبويب يكتب في `CachedSections` (Drift) عند مغادرته — `CasesRepository.saveSection`
  دالة عامة واحدة، تُستدعى من `_saveCurrentTabIfDirty` في `case_details_screen.dart` (حفظ عند
  الانتقال بين التابات + `PopScope` عند إغلاق الشاشة)
- [x] الأقسام العشرة المدعومة (التسعة + `support-recommendations`) تضيف أيضًا `enqueue` بالنوع
  الصحيح — مابر مخصّص لكل قسم في `lib/features/case_details/data/mappers/` يفصل شكل التخزين
  المحلي عن شكل جسم الـ `PUT` حرفيًا
- [x] `jsonDecode` لـ `selectedLivestockJson` — `AgricultureMapper.decodeSelectedLivestock`
  (`mainClassificationsJson` لم يُستهلَك بعد في شاشة القراءة — القسم يُقرأ محليًا من الكاش لا من
  استجابة الخادم مباشرة بعد؛ يُستكمل عند ربط تفريغ الاستجابة الفعلي)
- [x] تابان جديدان أُضيفا (لم يكونا موجودين قبل الفحص): **التصنيف الاجتماعي** و**الاحتياجات
  المُقيَّمة** — كانا قسمين مدعومين بالكامل في العقد بلا أي واجهة تعرضهما
- [x] `CaseDetailsRepositoryImpl` حقيقي يستبدل `MockCaseDetailsRepository` في المزوّد — يقرأ
  `basicInfo` من كاش `CasesRepository`، ويقرأ housing/utilities/initial-needs/classification/
  assessed-needs من `CachedSections` إن حُفظت محليًا سابقًا؛ **fallback مؤقت للـ Mock** لكل قسم لم
  يُبنَ له مابر بعد (أفراد الأسرة، المرفقات، الزيارة الميدانية، وكل الأقسام بلا endpoint) —
  موثّق بتعليق صريح في الكود، يُزال تدريجيًا
- [x] اكتُشفت فجوة حقيقية جديدة أثناء هذه المرحلة: تابا "التقييم الاجتماعي الحر" و"تفاصيل رأي
  الأخصائي" **مالهمش أي `PUT`/`POST` في العقد** — يُحفظان محليًا بلا مزامنة، وأُضيف طلب رسمي
  (**طلب 7**) في `BACKEND_CHANGE_REQUEST.md`
- [x] **أفراد الأسرة** — `CaseDetailsRepositoryImpl._familyFromCache` يقرأ من `CachedSections`
  بدل Mock؛ اختبار مخصّص في `case_details_repository_impl_test.dart`. الحقول الإحصائية (عدد
  التابعين، الأطفال...) تبقى `null` — غير معروضة في الشاشة أصلًا، والخادم لا يرجعها في `PUT
  /family-members` (§19)
- [x] **المرفقات** — `InitialNeedAttachmentsTab` أصبح `ConsumerStatefulWidget` يستهلك
  `pendingAttachmentsProvider`/`attachmentUploaderProvider` الحقيقيين (كانا موجودين وجاهزين من
  قبل، لكن التاب كان يستخدم قائمة `_pendingAttachments` محلية منفصلة تمامًا عن `PendingAttachments`
  في Drift). الالتقاط يستدعي `AttachmentUploader.capture` فعليًا ثم يحاول الرفع فورًا؛ عرض حالة كل
  مرفق (بانتظار/جارٍ الرفع/تم/فشل) من `UploadStage` الحقيقية، مع زر إعادة محاولة للفاشل
- [ ] بوابة الإكمال ١٠٠٪ قبل إتاحة زر الرأي — لم تُربَط بعد (الزر الحالي لا يزال `SnackBar` وهميًا،
  `_handleSubmitForReview` لم يُمَس في هذه الجولة)
- [ ] ربط `SyncStatusBadge` بالحالة الحقيقية من الطابور
- [ ] **المصيدة المالية** (راجع المرحلة ٢) — `saveSection` لتاب الدخل لا يعيد الجلب الاستباقي بعد
- [ ] `field_visit`, `field_verification` لا تزال تُقرأ من Mock داخل `CaseFullDetails` — طبقة
  البيانات لهما (`field_visits_api.dart`) جاهزة من قبل المرحلة ٣ لكنها غير مربوطة بشاشة التفاصيل
  نفسها (الزيارات الميدانية تُدار من شاشات منفصلة، لا من هذا الملف)

### المرحلة ٤ — شاشة التعارض ✅ **منجزة 2026-09-19** 🔴

> أهم شاشة في النظام. الأخصائي قضى يومًا في قرية نائية؛ هذه الشاشة تقرّر هل يضيع عمله.

- [x] `conflict_resolution_screen.dart` (687 سطرًا) — مربوطة من `sync_status_screen.dart:310`
- [x] عرض: قيمتك ↔ قيمة الخادم، حقلًا بحقل (`beneficiary` فقط — القسم الوحيد المتاح كاملًا عبر `GET /cases/{id}`)
- [x] ثلاثة خيارات صريحة: `_keepMine` · `_takeServer` · «ادمج يدويًا» (توجيه لتاب `case_details_screen.dart` المطابق عبر `_tabIndexForType`)
- [x] **لا دمج تلقائي ولا كتابة فوقية** (§14.3) — مطبَّق
- [x] رسالة توضيحية مستقلة لـ 422/403 (بلا مقارنة بيانات — الحالة نفسها تغيّرت لا محتواها)
- [ ] تحذير بسيط: `use_build_context_synchronously` عند `conflict_resolution_screen.dart:240` — ضمن ديون flutter analyze

### المرحلة ٥ — الميدان (الواجهة)

- [x] **شاشة قائمة الزيارات + نموذج تسجيل/تعديل زيارة ✅ منجزة 2026-09-19** —
  `lib/features/field_visits/presentation/field_visits_screen.dart` +
  `field_visit_form_screen.dart`، مدخل زر مستقل في `case_details_screen.dart`
  (بجانب زر "السجل") — شاشة منفصلة عن `CaseDetailsScreen` عمدًا، لأن
  `FieldVisitsRepository` يدعم قائمة زيارات متعددة للحالة الواحدة لا زيارة
  واحدة (راجع القرار في هذه المحادثة)
- [x] شاشة التقاط الصور — تُستخدَم بنية `AttachmentUploader`/`AddAttachmentSheet`
  الموجودة أصلًا من المرحلة ٣ (`fieldVisitLocalId` يربط الصورة بزيارتها)؛ نسخ
  فوري لمجلد دائم مطبَّق مسبقًا في `AttachmentUploader.capture`
- [x] **فحص حد 10 ميجا وقت الالتقاط** — `AttachmentPolicy.validate` (من
  المرحلة ٣، يُستدعى تلقائيًا عبر نفس `capture`)؛ **الضغط وقت الالتقاط تحديدًا
  لم يُنفَّذ بعد** (`image_picker` بجودة 85% فقط، لا ضغط إضافي مخصص)
- [x] نموذج الزيارة: `latitude`/`longitude` **كلاهما أو لا شيء** (§15.10) —
  مطبَّق في `FieldVisitsRepository.createLocal`/`updateLocalFields` (يُسقِط
  الناقص محليًا لا عند الإرسال)؛ التقاط GPS تلقائي عبر `LocationService`
  الجديد (`geolocator`)، اختياري بالكامل ولا يمنع الحفظ عند الفشل
- [ ] **شاشة التحقق الميداني — استبدال كامل** — **معلّقة على ردّ الباك إند
  (طلب 5 في `BACKEND_CHANGE_REQUEST_COMBINED.md`)**؛ بناؤها بالافتراض الحالي
  (لا `GET`) يُخاطر بمحو صامت لبيانات كل زيارة تحقق ثانية، فتُرجَّأ عمدًا
- [ ] خيار «الصور على Wi-Fi فقط» مفعّلًا افتراضيًا
- [ ] رفع على دفعات 2–3 متوازية (rate limit على `/init`) — **مطبَّق فعليًا
  للمرفقات العامة** (`AttachmentPolicy.maxConcurrentUploads` = 2 في
  `AttachmentUploader.uploadAllForCase`، من المرحلة ٣)؛ الزيارات نفسها ترفع
  صورها عبر نفس الآلية فلا عمل إضافي مطلوب هنا

**فجوة عقد جديدة اكتُشفت أثناء هذه المرحلة:** `PUT /field-visits/{id}` مقيَّد
بـ `SocialWorkerId` **منشئ الزيارة الأصلي**، لا بالأخصائي المسندة له الحالة
حاليًا — أخصائي جديد يستلم حالة يقدر يرى زيارات سابقة (بعد طلب 6) لكن لا
يقدر يعدّل فيها، فيضطر لإضافة زيارة موازية بدل تصحيحها. أُضيف **طلب 10** في
`BACKEND_CHANGE_REQUEST_COMBINED.md`. الشاشة بُنيت بالقيد الحالي (كل الزيارات
المعروضة محليًا قابلة للتعديل لأنها كلها من هذا الجهاز؛ القيد يظهر فعليًا فقط
مستقبلًا مع زيارات الزملاء القادمة عبر طلب 6).

### المرحلة ٦ — البحث والإشعارات

- [ ] ربط البحث بـ `search/cases` (الـ API جاهز)
- [ ] `notifications_api.dart` + `GET /notifications`
- [ ] `firebase_core` + `firebase_messaging` + ملفات الإعداد
- [ ] **تسجيل التوكن بعد كل دخول وكل استئناف** (§12 — الباك إند يُعطّل التوكن من أول فشل)

### المرحلة ٧ — بوابة الإطلاق

- [ ] **اختبار الميدان الكامل:** 9 أقسام + 7 صور + زيارة، **وضع الطيران طوال الوقت**، ثم تشغيل الشبكة → كل شيء يصل **مرة واحدة بالضبط**
- [ ] اختبار 409: جهازان على نفس الحالة
- [ ] اختبار إعادة الإسناد أثناء الأوفلاين → 403/422 يُعرَض بوضوح
- [ ] اختبار الطابور الأقدم من 7 أيام → إعادة دخول ثم تفريغ سليم
- [ ] **اختبار تكرار الزيارة:** قتل التطبيق أثناء `POST /field-visits` → لا زيارة مكرّرة
- [ ] `https://` مفعّل

---

## 5. مطلوب منك (حواجز خارج الكود)

1. **حساب `social_worker` حقيقي** + حالة مُسنَدة إليه على بيئة الاختبار
2. **جهاز حقيقي** — محاكي الأوفلاين لا يكفي لاختبار الميدان
3. **شهادة TLS** — `https://` (بنية تحتية، لا يُصلَح من التطبيق)
4. **ملفات إعداد Firebase** (`google-services.json` / `GoogleService-Info.plist`)
5. **قرارات الباك إند المعلّقة** في `BACKEND_CHANGE_REQUEST.md` — أهمها تمييز سبب الإرجاع (`returnInfo`)

---

## 6. الترتيب الموصى به (مُحدَّث بعد فحص 2026-09-19)

المراحل ١–٤ **منجزة بالفعل** (بما فيها شاشة التعارض، التي كانت مصنّفة خطأً 🔴 في نسخة سابقة من هذا
الملف قبل فحص `lib/features/sync/presentation/conflict_resolution_screen.dart`). الترتيب المتبقي
مبني على اعتماديتين حقيقيتين: (أ) الكود ↔ الكود، و(ب) الكود ↔ ردّ فريق الباك إند على
`BACKEND_CHANGE_REQUEST_COMBINED.md`.

```
   ✅ ١ (المحرّك) ──► ✅ ٢ (الكتابة) ──► ✅ ٣ (التفاصيل، جزئيًا) ──► ✅ ٤ (التعارض)
                                                                          │
                        ┌─────────────────────────────────────────────────┤
                        ▼                                                 ▼
              أ. إغلاق ديون المرحلة ٣                          ب. المرحلة ٥ (الميدان)
              (بوابة ١٠٠٪، SyncStatusBadge،                    شاشات التقاط/زيارة/تحقق —
               المصيدة المالية) — لا اعتماد خارجي،              طبقة البيانات جاهزة، تحتاج
               تبدأ فورًا                                       واجهة فقط. **تحتاج ردّ الباك
                        │                                        إند على طلب 5 (field-verification
                        │                                        GET) لتفادي إعادة كتابة شاشة
                        │                                        التحقق مرتين**
                        ▼                                                 │
              المرحلة ٦ (البحث/الإشعارات) ◄──────────────────────────────┘
              لا اعتماد على الباك إند — search/cases وFCM
              جاهزان في العقد، يمكن التوازي مع ٥ فورًا
                        │
                        ▼
              المرحلة ٧ (بوابة الإطلاق)
              **محجوزة** خلف: (١) اختبار تكرار الزيارة يحتاج طلب 7 أو 8
              من الباك إند (Idempotency-Key أو GET للمطابقة) وإلا البقاء
              على الحل المحلي المؤقت (dedupId)، (٢) `https://` مفعّلة
              (بنية تحتية، خارج الكود)
```

**الاعتمادية الفعلية على الباك إند** (تفصيلًا، حسب `BACKEND_CHANGE_REQUEST_COMBINED.md`):

| بند الخطة | يحتاج ردّ الباك إند على | لماذا |
|---|---|---|
| **المرحلة ٥** — شاشة التحقق الميداني | طلب 5 (`GET .../field-verification`) | بدونه: استبدال كامل بلا معرفة القيم الحالية = **محو صامت** بيانات كل زيارة ثانية. نبنيها بالافتراض الحالي (لا GET) نُخاطر بإعادة كتابتها لاحقًا |
| **المرحلة ٥** — شاشة الزيارة الميدانية | طلب 7/8 (`Idempotency-Key` أو `GET .../field-visits`) | بدونهما: نعتمد فقط على الحل المحلي المؤقت (`dedupId`)، وهو تخفيف لا ضمان — لا يمنع فقدان السجل عند تغيير الجهاز |
| **المرحلة ٣** — تبويب البيانات الأساسية | طلب 3 (8 حقول العنوان) | الحقول غائبة من العقد اليوم؛ لا شيء نربطه بها قبل إضافتها |
| **المرحلة ٣** — تابا التقييم الحر ورأي الأخصائي | طلب 4 | لا `PUT`/`POST` مطابق؛ الحفظ محلي فقط لحين الرد |
| **المرحلة ٧** — بوابة الإطلاق | طلب 1، 2 | شاشة الإرجاع وسير العمل بعد `return-for-completion` غير مؤكَّدين رسميًا |

**الخلاصة العملية:** المراحل غير المعتمِدة على الباك إند (إغلاق ديون ٣، البحث، الإشعارات/FCM) هي
الأولى بالتنفيذ **الآن** لأنها لا تنتظر أحدًا. شاشات الميدان (التقاط الصور، نموذج الزيارة نفسه بلا
التحقق) يمكن البدء فيها أيضًا فورًا — العالق تحديدًا هو **شاشة التحقق الميداني** فقط، لحين ردّ واضح
على طلب 5. بوابة الإطلاق (المرحلة ٧) هي الوحيدة المحجوزة فعليًا خلف رد خارجي بالكامل.
