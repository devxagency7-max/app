# الرد على BACKEND_CHANGE_REQUEST_3.md — طلب 11

**التاريخ:** 2026-09-19
**الحالة:** ✅ تم التنفيذ والنشر على السيرفر (`http://187.77.70.246`)

---

## 🔴 طلب 11 — صلاحية `create_case` لدور `social_worker`

**تم القبول والتنفيذ بالكامل.**

`social_worker` أصبح يملك صلاحية `create_case` الآن. يقدر ينادي `POST /api/v1/cases` مباشرة من التطبيق بنفس الـ contract الموثّق حاليًا — نفس الحقول، نفس الـ validation، نفس استجابة `draft` بالضبط. مفيش أي تغيير في شكل الـ request أو الـ response.

`RolePermissionMatrix.cs` اتعدّل، والتوثيق (`FLUTTER_API_DOCUMENTATION.md` §7.1/§7.2 و`WEB_API_DOCUMENTATION.md` المقابل) اتحدّث ليعكس ده.

### الأسئلة التابعة

**1) الإسناد التلقائي بعد الإنشاء**

**لسه لأ — مش متفّعل حاليًا.** الحالة بتتعمل بحالة `draft` زي ما هي موثّقة، وبتفضل غير مُسندة (unassigned) لحد ما حد يستدعي `POST /cases/{id}/assign` بشكل منفصل.

فيه نقطة مهمة هنا: امتلاك `create_case` **مش كافي** عشان تنادي `assign` — الـ endpoint ده عنده allow-list منفصل (defense-in-depth) في الـ workflow engine نفسه، مقصور حاليًا على `data_entry`، `manager`، `reviewer` فقط. يعني حتى لو الأخصائي أنشأ الحالة، مش هيقدر يسندها لنفسه بمحاولة استدعاء `assign` مباشرة — هيرجّع 403 برضو.

الإسناد التلقائي للمُنشئ قرار منتج زيادة عن الطلب الأصلي، ومحتاج تنفيذ فعلي (مش مجرد صلاحية) — إضافة الأخصائي كـ assignee جوه نفس الـ handler بتاع `CreateCaseCommand`، وربما توسيع allow-list الخاص بـ workflow engine. **نفضّل ناخد قرار منكم بشكل صريح على ده قبل ما ننفذه**، لأنه بيغيّر سلوك موجود (الحالة بتفضل unassigned لحد الآن)، مش مجرد فتح باب كان مقفول. لو موافقين، هنجهزه في دفعة تعديل تانية.

**2) مركز/قرية غير مسجّلين**

مفيش مسار حاليًا لإضافة قرية جديدة من التطبيق نفسه. الحقل `villageId` مقفول على القائمة المعتمدة فقط (لازم ينتمي لـ `centerId` المحدد، وإلا 422 "القرية غير تابعة لهذا المركز"). إضافة قرية جديدة محتاجة `manage_locations`، وهي صلاحية الأخصائي معهوش أي نية حاليًا نضيفها ليه (منطقيًا دي بيانات مرجعية إدارية، مش حاجة بتتغير في الميدان بشكل يومي). لو فيه حاجة فعلية بتحصل كتير (قرى جديدة بتظهر بشكل متكرر)، محتاجين نناقشها كطلب منفصل — مش حل سريع في نفس الطلب ده.

**3) حدود الاستخدام (rate limiting)**

مفيش حد أقصى مُطبّق من جانبنا حاليًا على عدد الحالات الجديدة اللي الأخصائي يقدر ينشئها في اليوم. مش رافضين الفكرة، بس مفضّلين ننتظر نشوف الاستخدام الفعلي الأول قبل ما نحدد رقم تعسفي. لو حابين تطبّقوها من الواجهة (زي ما اقترحتوا)، إحنا معاكم — بس لو محتاجين إنفاذ من السيرفر (server-side enforcement) في وقت لاحق، ابعتولنا الرقم المقترح ونضيفه كـ rate limit فعلي.

---

## ملخص التنفيذ

- `src/Nahda.SharedKernel/Authorization/RolePermissionMatrix.cs` — `social_worker` role دلوقتي يملك `create_case`.
- التوثيق (Flutter + Web) اتحدّث في كل الأماكن اللي كانت بتقول إن `social_worker` معهوش الصلاحية دي.
- الكود اتبنى وتم التحقق منه (`dotnet build`)، وتم نشره على السيرفر (`187.77.70.246`) — الحاوية الجديدة شغالة وصحية (healthy)، مفيش downtime حصل أثناء النشر.
- ملحوظة جانبية: لقينا commit سابق على main كان ناقص كلاس (`Pagination`) استخدمه كود متعمول له commit بالفعل، فكان بيكسر أي build جديد من الصفر. اتصلح كجزء من نفس عملية النشر دي (commit منفصل، مفيش تغيير في السلوك).

في انتظار ردكم على السؤال (1) قبل ما نبدأ فيه، وفي انتظار باقي طلبات `BACKEND_CHANGE_REQUEST_COMBINED.md`.
