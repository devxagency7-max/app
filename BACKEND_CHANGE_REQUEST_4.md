# طلب تعديل على الـ API — الدفعة الرابعة

**التاريخ:** 2026-09-19
**المرجع:** ردّكم `BACKEND_CHANGE_RESPONSE_3.md` — السؤال التابع رقم (1)
**السياق:** تأكيد صريح على السؤال المعلّق بخصوص الإسناد بعد إنشاء الحالة، زي ما طلبتوا قبل ما تنفّذوا.

---

## 🔴 طلب 12 — إسناد تلقائي للأخصائي عند إنشائه حالة جديدة

### القرار

**نعم، مطلوب.** الحالة اللي الأخصائي ينشئها بنفسه (`POST /cases`) المفروض تتسند له **تلقائيًا** فور الإنشاء — بلا خطوة `assign` منفصلة، وبلا تدخّل من `data_entry`/`manager`/`reviewer`.

### السبب

منطقيًا: لو الأخصائي هو اللي قابل المستفيد في الميدان وسجّل الحالة بنفسه، هو أصلًا "المُسنَد الطبيعي" لها — مفيش داعي لحلقة إضافية (يرجع يبلّغ حد يسندها له) كانت أصلًا المشكلة اللي طلب 11 اتعمل عشان نتخطّاها. حالة `draft` غير مُسندة لحد ما يتدخّل طرف تالت تُعيد نفس الاحتكاك اللي كنا بنحاول نلغيه.

### المطلوب تقنيًا

في نفس `handler` بتاع `CreateCaseCommand`: لو الطالب (caller) عنده `create_case` **و**هو `social_worker` تحديدًا، يتسجّل كـ`AssignedToUserId` مباشرة وقت الإنشاء، والحالة تخرج بحالة تعكس ده (زي ما وضّحتوا، ممكن تفضل `draft` أو تتقدّم لحالة تانية — القرار ده ليكم حسب الـworkflow الداخلي، المهم إنها تبقى **مُسندة** لا `unassigned`).

بخصوص allow-list الـ`assign` endpoint المنفصل (`data_entry`/`manager`/`reviewer`): **مش محتاجين نضيف `social_worker` ليه** — الإسناد هنا بيحصل داخليًا في نفس عملية الإنشاء، مش عبر استدعاء الأخصائي لـ`assign` بنفسه. الـallow-list الحالي يفضل زي ما هو.

### باقي الأسئلة (2 و3)

مفيش عندنا احتياج فوري عليهم حاليًا — هنرجعلكم بيهم لو ظهرت حاجة فعلية من الاستخدام الميداني. شكرًا على التوضيح.

---

في انتظار تنفيذكم لده عشان نقدر نبني زرار "إنشاء حالة جديدة" في التطبيق بثقة — بناؤه قبل ده هيسيب الحالة الجديدة معلّقة (unassigned) والأخصائي مش هيقدر يشتغل عليها فورًا، وهو بالظبط الاحتكاك اللي طلب 11 كان قصده يتلغي.
