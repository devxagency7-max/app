# رد الباك إند على طلبات فريق الموبايل (Flutter)

**بالرد على:** `BACKEND_CHANGE_REQUEST.md` بتاريخ 2026-09-18
**تاريخ هذا الرد:** 2026-09-19
**الحالة:** **جميع الطلبات الستة نُفّذت** في الكود، وأُضيفت اختبارات لكل واحد منها، والاختبارات الحالية (1290 اختبار) لم يُكسر أي منها. لم يُعمل commit ولا push بعد — التغييرات في working tree جاهزة للمراجعة قبل الدمج.

قبل ما ننفّذ أي طلب، رجعنا للكود الفعلي (مش وثّقنا افتراضًا) — وده كشفلنا حاجتين إضافيتين مهمتين لكم موجودين تحت.

---

## 🔴 طلب 1 — `returnInfo` لتمييز المراجع من المدير

**تم التنفيذ بالكامل.**

`GET /api/v1/cases/{id}` بيرجع دلوقتي:

```json
{
  "id": "5b1e2a3c-...",
  "status": "returned_to_worker",
  "returnInfo": {
    "returnedBy": "reviewer",
    "returnedByName": "أ. حسام الدين",
    "reason": "برجاء استكمال بيانات السكن",
    "returnedAtUtc": "2026-09-18T10:15:00Z"
  }
}
```

أو `"returnInfo": null` لو الحالة مش في وضع إرجاع.

نفس الحقل المسطّح `"returnedBy": "reviewer" | "manager" | null` اتضاف لعناصر:
- `GET /dashboard/work-queue`
- `GET /cases`

**تفصيلة تقنية مهمة (ليست مشكلة، لكن يفيدكم تعرفوها):** بدل ما نضيف عمود جديد في جدول `cases` (نسخة تانية من بيانات موجودة أصلاً، ومعرضة لعدم التطابق)، الحقل ده بيتحسب من **سجل الـ timeline** الموجود أصلاً (append-only) — كل عملية إرجاع (من المراجع أو المدير) بتكتب صف واحد فيه مين وإمتى وليه. بنقرأ **آخر** صف إرجاع. ده معناه:
- شغّال على **كل الحالات القديمة** من غير أي migration بيانات.
- لو حصل إرجاع → استكمال → إرجاع تاني (حتى من الطرف التاني)، الـ `returnInfo` بيتحدّث صح في كل مرة.

**ملحوظة:** حقل `opinion.ReturnReason` اللي كنتوا بتشيروا له في طلبكم فضل زي ما هو (بتاع رأي المراجع نفسه)، لكنه **مش** المصدر اللي بيتحسب منه `returnInfo.reason` — لأنه بيتمسح/يتكتب فوقه في كل دورة مراجعة ومفيهوش نسخة بتاعة المدير أصلاً. استخدمنا الـ timeline لأنه المصدر الوحيد اللي فعلاً بيعكس "آخر إرجاع، مهما كان مصدره".

---

## 🔴 طلب 2 — حالة `return-for-completion` بتاعة المدير

**اكتشفنا إن السؤال ده كشف باج حقيقي، مش مجرد نقص توثيق.**

قبل التعديل: `POST /cases/{id}/return-for-completion` كان بينقل الحالة لـ `pending_review` (مش `returned_to_worker`). ده كان معناه عمليًا:
- الأخصائي **معندوش أي إجراء متاح** على الحالة دي (`FieldVisitAuthorization.AllowedStatuses` بتشمل `returned_to_worker` بس).
- الحالة كانت بترجع للمراجع بدل الأخصائي — يعني حلقة مقفولة بين المدير والمراجع من غير ما الأخصائي يقدر يتدخل خالص.

**التعديل:** `return-for-completion` بقى بينقل الحالة لـ **`returned_to_worker`** — نفس حالة إرجاع المراجع بالظبط. ده اللي بيخلي الأخصائي يقدر يستكمل ويرسل تاني، وده اللي بييجي بعده طلب 1 يفرّق مين اللي رجّعها فعلًا.

**باج تاني اتكشف واتصلح أثناء التنفيذ:** الحالة المرتجعة (من أي مصدر) كانت **مينفعش تتقدّم للمراجعة تاني** — كانت بترجع `422 OPINION_SLOT_LOCKED` لو الأخصائي حاول يعيد الإرسال بعد الإرجاع. اتصلح: الإرجاع دلوقتي بيلغي دورة المراجعة القديمة، فالأخصائي يقدر يعيد الإرسال بشكل طبيعي.

**إضافة:** `return-for-completion` بقى بيقبل حقل `reason` **اختياري** في الـ body (زي إرجاع المراجع بس مش إجباري، عشان مكسرش أي كولر قديم مبعتش الحقل ده):

```json
POST /cases/{id}/return-for-completion
{
  "caseRowVersion": 7,
  "reason": "برجاء مراجعة قيمة الدعم المقترحة"
}
```

لو اتبعت، هيظهر في `returnInfo.reason`. لو متبعتش، `returnInfo.reason` هيرجع `null` وباقي الحقول هترجع عادي.

---

## 🔴 طلب 3 — 8 حقول العنوان للمستفيد

**تم التنفيذ بالكامل.** الحقول التمانية دي اتضافت زي ما طلبتوها بالظبط (كلها nullable، من غير حد أقصى جديد على أي حقل موجود):

| الحقل | النوع | الحد |
|---|---|---|
| `email` | string? | 255 |
| `street` | string? | 255 |
| `buildingNumber` | string? | 50 |
| `floor` | string? | 50 |
| `apartmentNumber` | string? | 50 |
| `landmark` | string? | 255 |
| `area` | string? | 255 |
| `employer` | string? | 255 |

متاحة في `PUT /cases/{id}/beneficiary` و `GET /cases/{id}`. فيه EF migration واحدة إضافية (`AddBeneficiaryContactAndAddressFields`) — 8 أعمدة nullable بس، مفيهاش أي تعديل هدّام على بيانات موجودة.

**تفصيلتين مهمين قبل ما تبنوا عليهم:**
1. **`employer` مختلف عن `job` الموجود أصلاً.** `job` هي المسمى الوظيفي، `employer` هي جهة العمل نفسها. الحقلين مستقلين — ممكن يكون عند المستفيد وظيفة من غير جهة عمل مسجلة أو العكس.
2. **`street/buildingNumber/floor/...` مش بتحل محل حقل `address` الموجود.** `address` فضل زي ما هو — سطر نصي حر بيستخدمه الويب حاليًا. الحقول الجديدة دي **تفصيل إضافي** بيملاه الأخصائي في الميدان، ومفيش تحويل تلقائي بين الاتنين. لو حبيتوا تعرضوا عنوان موحّد في الواجهة، هتحتاجوا تجمّعوا الحقول دي بأنفسكم على الفرونت.

**تأكيد:** الحقول دي **مش داخلة** في حساب `CaseCompletionCalculator` — نسبة الإكمال للحالات القائمة مش هتتأثر خالص.

---

## 🟡 طلب 4 — `Idempotency-Key` على تسجيل الزيارة الميدانية

**تم التنفيذ بالكامل، بنفس آلية الـ9 مسارات الموجودة بالظبط (نفس جدول التخزين، نفس مدة الـ24 ساعة، نفس النطاق `(key, userId)`).**

```
POST /api/v1/cases/{caseId}/field-visits
Idempotency-Key: <uuid>
```

**نقط مهمة جدًا لازم فريقكم يعرفها بالظبط:**

1. **الهيدر اختياري فعلاً** زي ما طلبتوا — أي طلب من غير الهيدر (أو فيه قيمة مش UUID صالح) بيتصرف **بالظبط** زي دلوقتي: زيارة جديدة في كل مرة، من غير أي حجز أو إعادة تشغيل.
2. **المفتاح لازم يكون UUID صالح** (مش أي نص عشوائي) — لو بعتوا قيمة مش UUID، هيتعامل معاها كأنها مفيش هيدر خالص (مش هيرجع خطأ 400، عشان التوافق مع العملاء الحاليين).
3. **آمن ضد الـ race فعليًا:** لو التطبيق بعت نفس الطلب مرتين في نفس اللحظة (زي ما بيحصل لما الشبكة تتقطع أثناء الانتظار)، الاتنين مش هيدخلوا صف مكرر — قاعدة البيانات بتحسم مين اللي "كسب" الحجز، والتاني بيستنى شوية وبيرجع نفس الرد.
4. **فشل الطلب (403/404/422/500) بيحرّر المفتاح تلقائيًا** — يعني تقدروا تعيدوا المحاولة بنفس المفتاح لو الطلب فشل، مش هيفضل "متجمد" لمدة يوم كامل. **بس النجاح فقط هو اللي بيتم إعادة تشغيله (replay)** — يعني ما فيش صف زيارة تاني ولا سجل تدقيق (audit) تاني بيتكتب.
5. **مفتاح من مستخدم لمستخدم تاني مش هيرجّع بيانات حد تاني** — كل مفتاح مربوط بالمستخدم اللي بعته، فمفتاح مستخدم A ما لوش أي تأثير عند مستخدم B.

**نصيحة عملية للتنفيذ عندكم:** ولّدوا **مفتاح UUID واحد وقت ما الزيارة تتحط في طابور الإرسال (enqueue) أوفلاين** — مش وقت كل محاولة إرسال — وأعيدوا استخدام **نفس المفتاح** في كل إعادة محاولة لنفس الزيارة دي لحد ما تنجح. لو ولّدتوا مفتاح جديد في كل محاولة، الحماية دي مش هتفيدكم خالص.

---

## 🟢 طلب 5 — توحيد سلوك الـ pagination

**تم التنفيذ.** `page=0` أو أي رقم صفحة سالب دلوقتي بيتصحح تلقائيًا لـ`page=1` في **كل** الـ endpoints (مش الأربعة اللي ذكرتوهم بس — عملناها من مكان واحد مشترك (`Pagination.NormalizePage`) فبقت متسقة في كل مكان بيستخدم pagination في الـ API كله، مش الأربعة المذكورين بس). مفيش 422 بعد كده على `page=0`.

القيم غير الرقمية أو الأنواع الخاطئة لسه بترجع خطأ `422 VALIDATION_ERROR` عادي — الاستثناء بس هو تحديدًا `page=0` والأرقام السالبة.

---

## 🟢 طلب 6 — إرجاع المصفوفات كمصفوفات

**تم التنفيذ — بالطريقة الآمنة اللي اقترحتوها بالظبط.**

`PUT /cases/{id}/agriculture` و`PUT /cases/{id}/classification` بيرجعوا دلوقتي **الحقلين الاثنين مع بعض**:

```json
{
  "selectedLivestockJson": "[\"cows\",\"other\"]",
  "selectedLivestock": ["cows", "other"],

  "mainClassificationsJson": "[\"فقر\",\"إعاقة\"]",
  "mainClassifications": ["فقر", "إعاقة"]
}
```

الحقول القديمة (`*Json`) **فضلت زي ما هي** عشان الويب مكسرش (بيستخدمها فعلاً). استخدموا الحقول الجديدة (`selectedLivestock`, `mainClassifications`) — دول arrays حقيقية في الـ JSON مش نصوص، اتأكدنا من ده باختبار مخصص بيفحص الـ response body فعليًا.

---

## ❓ إجابات الأسئلة الستة (من الكود الفعلي، مفيش تخمين)

**1. فلاتر `GET /cases`:** مؤكد — الفلاتر الوحيدة فعليًا هي `status` و`bookmarked`. مفيش نية لإضافة `assignedTo` حاليًا. اعتمدوا نهائيًا على `GET /dashboard/work-queue` لحالات الأخصائي — لكن انتبهوا: work-queue **أضيق** من "كل الحالات المُسندة لي"، لأنه بيستبعد الحالات في `pending_review`/`pending_approval` (دي مش "شغل الأخصائي" في اللحظة دي). لو محتاجين "كل حالاتي بأي حالة"، الطلب ده لسه مش موجود — قولولنا لو محتاجينه فعلًا ونناقشه في جولة تانية.

**2. `appliances[].applianceKey`:** **لا يوجد enum ثابت في الباك إند** — الحقل حر (free-form) فعلًا، بحد أقصى 30 حرف. أي "قوائم" شكلها ثابتة في الفرونت (زي `fridge`, `tv`) دي مجرد اتفاق ضمني مش مفروض من السيرفر. لو محتاجين قائمة معتمدة فعلًا لعرض أسماء عربية صح أوفلاين، ده queued كطلب مستقل — الحل الأنسب غالبًا يكون عن طريق نظام الـ Configuration/Dropdowns الموجود أصلاً (`GET /dropdowns/{key}`)، مش enum مكوّد.

**3. `assessedNeeds[].source` و`.status`:** **لا يوجد enum ثابت لأي منهما حاليًا.** لو محتاجين قيم معتمدة، لازم نتفق عليها كطلب منفصل ونضيفها بشكل صريح (مش نخترعها دلوقتي من غير سياق عمل واضح).

**4. حدّ المرفقات:** **لا يوجد حد أقصى لعدد المرفقات في الحالة الواحدة ولا حد تخزين إجمالي.** الموجود فعليًا بس: 10 ميجا للملف الواحد، 7 أنواع MIME مسموحة، فحص magic bytes، وتنظيف تلقائي للمرفقات المهجورة (اللي اتعمللها init ومتعملهاش commit) بعد 48 ساعة. الـ10-15 صورة للزيارة الميدانية مفيش أي مانع تقني منها.

**5. صلاحية `uploadUrl`:** **30 دقيقة** بالضبط (مش 15 دقيقة — دي مدة صلاحية رابط الـ**تنزيل** `download`، وهي قيمة مختلفة). لو الصورة اتلقطت أوفلاين وعدّت الـ30 دقيقة قبل الرفع، هتحتاجوا تعملوا `/init` تاني للحصول على رابط جديد.

**6. `caseId` في الإشعارات:** **مضمون فعلًا لكل إشعار متعلق بحالة** — فيه مسار إنشاء واحد بس للإشعارات، وهو بيتخطى (skip) أي حالة بيانات ناقصة أو محذوفة بدل ما ينشئ إشعار بـ`caseId` فاضي. العمود نفسه نوعه nullable في قاعدة البيانات (لأسباب تقنية عامة)، فننصحكم تعملوا null-check دفاعي في الكود عندكم، لكن من ناحية العقد الفعلي: أي إشعار "متعلق بحالة" هيوصلكم ومعاه `caseId` دايمًا.

---

## ملاحظة أخيرة مهمة

اكتشفنا كمان إن الـ`response_body` المخزّن لإعادة التشغيل (replay) بتاع الـ`Idempotency-Key` بيترجع **JSON متطابق في المحتوى** لكن **مش بالضرورة نفس الـbytes حرفيًا** (نفس السلوك موجود في الـ9 مسارات الأصلية أصلًا، مش حاجة جديدة). يعني: قارنوا الاستجابات بالـ**تحليل والمقارنة المنطقية** (parse ثم compare)، مش بمقارنة النص الخام حرف بحرف، لو عندكم أي اختبار أو منطق أوفلاين بيعتمد على تطابق الرد.

---

**الحالة:** جاهز للمراجعة والدمج من جانبنا. قولولنا لو محتاجين توضيح إضافي على أي نقطة، أو لو ظهر أي سلوك مختلف عن الموضّح هنا وقت التكامل الفعلي.
