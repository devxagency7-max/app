# طلب تعديل على الـ API — الدفعة الثالثة

**التاريخ:** 2026-09-19
**المرجع:** `FLUTTER_API_DOCUMENTATION.md` §7.1، §7.2، `POST /api/v1/cases`
**السابق:** `BACKEND_CHANGE_REQUEST.md` (18 سبتمبر) — ردّ عليه فريقكم في `BACKEND_CHANGE_RESPONSE.md` (19 سبتمبر)، شكرًا على السرعة. `BACKEND_CHANGE_REQUEST_COMBINED.md` (19 سبتمبر) لسه بانتظار الرد على الطلبات المتبقية فيه.

**طلب واحد فقط.** منفصل عمدًا في ملف مستقل عشان ميضيعش وسط باقي الطلبات — وهو قرار منتج قبل ما يكون قرار كود.

---

## 🔴 طلب 11 — صلاحية `create_case` لدور `social_worker`

### المشكلة

الوثيقة توثّق بوضوح (§7.1) إن `social_worker` معندوش صلاحية `create_case`:

> Notably **absent**: `create_case` (a worker cannot create a new case from the app)

وإن `POST /api/v1/cases` هيرجّع `403 FORBIDDEN` لأي محاولة من `social_worker` (§20، توثيق الـendpoint نفسه):

> `403 FORBIDDEN` — actor lacks `create_case` (e.g. social_worker).

هذا القرار كان منطقي وقت التصميم الأصلي (الأخصائي يستقبل حالات مُسندة له من `data_entry`/`manager`، مش بينشئها بنفسه). لكن **من واقع الاستخدام الفعلي في الميدان**، محتاجين نعيد فتح النقاش دي.

### المطلوب

إضافة صلاحية `create_case` لدور `social_worker`، بحيث يقدر ينشئ حالة جديدة (`POST /api/v1/cases`) مباشرة من التطبيق، بنفس شكل الـrequest الموثّق حاليًا للأدوار التانية — بلا أي تغيير في العقد نفسه (نفس الحقول، نفس الـvalidation، نفس استجابة `draft`).

### السبب

الأخصائي بيقابل حالات جديدة فعليًا وهو في الميدان (مستفيد مش مسجّل عندكم أصلًا، عائلة جاره لقاها محتاجة مساعدة أثناء زيارة تانية). النظام الحالي بيفرض عليه يرجع لمكتب/يبلّغ `data_entry`/`manager` يسجّلوا الحالة الأول قبل ما يقدر يبدأ شغله عليها — ده تأخير حقيقي وفقدان فرصة توثيق فورية وهو لسه واقف قدام المستفيد.

### توضيح إضافي — أي دور ينشئ الحالة؟

بما إن قفل المنصة (§2.2) بيمنع أي دور غير `social_worker` من تسجيل الدخول عبر الموبايل أصلًا (`403 PLATFORM_NOT_ALLOWED`)، فإن **أي مستخدم بيستخدم تطبيق الموبايل هو `social_worker` دائمًا** — مفيش سيناريو تاني. يعني المطلوب فعليًا: "خلّوا الدور الوحيد المسموح له بالدخول من الموبايل يقدر ينشئ حالة"، مش تغيير في مصفوفة الأدوار العامة (`manager`/`reviewer`/`data_entry` يفضلوا زي ما هم، بيشتغلوا من الويب أصلًا).

### أسئلة تابعة لو الطلب اتقبل

1. **الإسناد بعد الإنشاء:** الحالة بتتولد بحالة `draft` (§20). هل الأخصائي المُنشئ يتسند تلقائيًا لنفسه، ولا لازم استدعاء `POST /cases/{id}/assign` منفصل (وده بيحتاج `create_case` برضو حسب التوثيق — يبقى نفس الصلاحية كافية)؟ نفضّل **الإسناد التلقائي للمُنشئ** لو ده متاح من جانبكم، يوفّر خطوة ثانية غير ضرورية في الميدان.
2. **مركز/قرية غير مسجّلين:** لو الأخصائي في قرية مش موجودة في قائمة `villageId` المعتمدة عندكم، إيه المسار المتاح؟ هل فيه مسار لإضافة قرية جديدة، ولا الحقل مقفول على القائمة الحالية فقط؟
3. **حدود الاستخدام:** هل تفضّلوا نضيف حدًّا (مثلًا: عدد حالات جديدة كحدّ أقصى في اليوم لكل أخصائي) كإجراء احترازي أول ما تُفعَّل الصلاحية، تجنّبًا لسوء استخدام غير مقصود؟ نقدر نطبّقه من جانبنا في الواجهة لو حددتوا رقمًا.

### الأولوية

🔴 — ده مش عطل تقني، لكنه قرار منتج بيوقف قدرة الأخصائي على توثيق حالة جديدة فور ما يقابلها، وهو صميم الاستخدام الميداني للتطبيق.

---

شكرًا لمراجعتكم السريعة على الدفعة الأولى — في انتظار رأيكم في الطلب ده، وفي باقي طلبات `BACKEND_CHANGE_REQUEST_COMBINED.md`.
