# طلبات تعديل على الـ API — رسالة موحّدة (تجميع الدفعتين السابقتين)

**التاريخ:** 2026-09-19
**المرجع:** `FLUTTER_API_DOCUMENTATION.md`
**السياق:** التطبيق Offline-First بالكامل — الأخصائي يعمل بلا اتصال في الميدان، وكل شيء يُزامَن تلقائيًا عند عودة الشبكة. هذه الرسالة تدمج طلبين سابقين لم يردنا رد عليهما بعد (**BACKEND_CHANGE_REQUEST.md** بتاريخ 18 سبتمبر، **BACKEND_CHANGE_REQUEST_2.md** بتاريخ 19 سبتمبر) في مكان واحد مرتّب بالأولوية الحقيقية، حتى لا يضيع شيء بين ملفين.

**9 طلبات إجمالاً** (أُضيف طلب 10 بتاريخ 2026-09-19 أثناء بناء شاشة الزيارات الميدانية في التطبيق). ستة منها 🔴 توقف شاشات فعليًا أو تُفقد بيانات في الاستخدام العادي — نرجو الرد عليها تحديدًا أولًا ولو بشكل أولي، حتى لو باقي الطلبات احتاجت وقتًا أطول.

---

## ترتيب الأولوية الكامل

| # | الطلب | الأولوية | الأثر |
|---|---|:---:|---|
| 1 | `returnInfo` لتمييز مصدر الإرجاع (مراجع/مدير) | 🔴 | شاشة الإرجاع تعرض معلومة خاطئة |
| 2 | توضيح ناتج `return-for-completion` على `status` | 🔴 | قد يمنع الأخصائي من العمل على حالة أرجعها المدير |
| 3 | 8 حقول مفقودة للمستفيد (عنوان تفصيلي) | 🔴 | تبويب البيانات الأساسية غير مكتمل ميدانيًا |
| 7 (دفعة 1) | مسار حفظ للتقييم الاجتماعي الحر + تفاصيل رأي الأخصائي | 🔴 | تابان يُحفظان محليًا فقط، لا يصلان لأي مستخدم آخر |
| 8 (دفعة 2) | `GET /cases/{id}/field-verification` | 🔴 | **محو صامت** لبيانات التحقق في كل حالة تُزار مرتين فأكثر |
| 7 (دفعة 2) | `GET /cases/{id}/field-visits` | 🔴 | فقدان سجلّ الزيارات عند تغيير الجهاز + تعطّل التعافي من انقطاع الشبكة |
| 10 (جديد) | تعديل الزيارة مقيَّد بمنشئها لا بالأخصائي الحالي للحالة | 🟠 | أخصائي جديد يستلم حالة لا يقدر يكمّل زيارة زميله — يضيف زيارة موازية بدل تصحيحها |
| 4 (دفعة 1) | `Idempotency-Key` على `POST /field-visits` | 🟡 | يقلّل خطر تكرار الزيارات (لا يمنعه بدون هذا) |
| 5 (دفعة 1) | توحيد سلوك الـ pagination (`page=0`) | 🟢 | مصدر أخطاء دائم، لا يعطّل |
| 6 (دفعة 1) | إرجاع المصفوفات كمصفوفات لا نصوص JSON | 🟢 | نتعامل معه بـ `jsonDecode`، لكنه يخالف اتساق العقد |

> ملاحظة على 4/7(دفعة2): الطلبان يحلّان نفس المشكلة (تكرار الزيارات) من طرفين مختلفين — `Idempotency-Key` يمنعها من الأساس، و`GET` يكشفها بعد وقوعها فنتعافى منها. تنفيذ أحدهما يخفّف الإلحاح لكن لا يُغني عن الآخر: `GET` وحده لازم أيضًا لحل فقدان سجلّ الزيارات عند تغيير الجهاز، وهي مشكلة منفصلة تمامًا لا علاقة لها بالتكرار.

---

## 🔴 طلب 1 — تمييز مصدر الإرجاع (مراجع / مدير)

**المشكلة:** الحالة المرتجعة من المراجع (`POST /cases/{id}/return-to-worker`) والمرتجعة من المدير (`POST /cases/{id}/return-for-completion`) تصلان بنفس القيمة تمامًا: `status: "returned_to_worker"`. لا وسيلة للتفريق. الفرق تشغيلي حقيقي: إرجاع المراجع غالبًا نقص بيانات (استكمال وإعادة إرسال)، إرجاع المدير غالبًا مراجعة قرار (يحتاج تنسيقًا مع رئيس الوحدة).

**المطلوب:** إضافة `returnInfo` إلى استجابة `GET /api/v1/cases/{id}`:
```json
"returnInfo": {
  "returnedBy": "reviewer",
  "returnedByName": "أ. حسام الدين",
  "reason": "برجاء استكمال بيانات السكن",
  "returnedAtUtc": "2026-09-18T10:15:00Z"
}
```
`null` عندما لا تكون الحالة مرتجعة. يعكس **آخر** عملية إرجاع فقط.

**البديل الأبسط:** حقل `returnedBy` فقط (`"reviewer"`/`"manager"`) يحلّ 80% من المشكلة.

**إضافة مطلوبة:** نفس المعلومة (حقل `returnedBy` مسطّح) في عناصر `GET /dashboard/work-queue` و`GET /cases` — الأخصائي يحتاج يرى الفرق في القائمة قبل فتح الحالة.

---

## 🔴 طلب 2 — توضيح ناتج `return-for-completion` على `status`

**المشكلة:** الوثيقة توضّح أن `return-to-worker` ينقل الحالة إلى `returned_to_worker`. لكن ناتج `return-for-completion` على `status` غير موثّق بنفس الوضوح.

**المطلوب:** تأكيد مكتوب: (أ) ينقل لـ`returned_to_worker` نفسها، أو (ب) حالة أخرى — وما هي؟ مع إضافة السطر المقابل لجدول الانتقالات في الوثيقة.

**لماذا مهم:** `FieldVisitAuthorization.AllowedStatuses` تشمل `ReturnedToWorker`. لو `return-for-completion` ينتج قيمة مختلفة، **يصبح الأخصائي عاجزًا عن تسجيل زيارة أو رأي على حالة أرجعها المدير**.

---

## 🔴 طلب 3 — 8 حقول مفقودة للمستفيد

**المشكلة:** حقول موجودة في النموذج الورقي المعتمد وتُملأ ميدانيًا، غائبة من `PUT /cases/{id}/beneficiary` و`GET /cases/{id}`:

| الحقل | النوع | الحد |
|---|---|---|
| `email` | string? | 255 |
| `street` | string? | 255 |
| `buildingNumber` | string? | 50 |
| `floor` | string? | 50 |
| `apartmentNumber` | string? | 50 |
| `landmark` | string? | 255 |
| `area` | string? | 255 |
| `employer` | string? | 255 |

كلها اختيارية — لا تكسر أي عميل قائم. **رجاءً لا تدخل في حساب `CaseCompletionCalculator`** حتى لا ترتفع متطلبات الـ100% على الحالات القائمة.

**السبب:** العنوان التفصيلي ضروري للوصول للمستفيد في القرى، حيث "المركز + القرية" لا تكفي.

---

## 🔴 طلب 4 — مسار حفظ للتقييم الاجتماعي الحر + تفاصيل رأي الأخصائي

**المشكلة:** اكتُشف أثناء ربط شاشة التفاصيل بالبيانات الحقيقية أن تابين يكتب فيهما الأخصائي **لا يقابلهما أي `PUT`/`POST` في العقد**:

**١. التقييم الاجتماعي الحر:** `familySituation`, `economicSituation`, `housingSituation`, `strengths`, `mainProblems`, `overallAssessment` (كلها string?)، `needLevel` (string، إلزامي).

**٢. تفاصيل رأي الأخصائي:** `opinion`, `caseSummary`, `assessment`, `reasons`, `recommendation` (string)، `additionalNotes` (string?).

العقد عنده فقط `POST /cases/{id}/opinions/worker` بحقلين (`decision`, `notes` ≤2000 حرف) — لا مكان لخمس حقول تفصيلية.

**الأثر:** الشاشة تعرض هذين التابين كأنهما محفوظان رسميًا. **نحفظهما محليًا فقط اليوم** — حل مؤقت يمنع فقد العمل، لكن لا يصل للمراجع أو المدير ولا يظهر في تقارير الخادم.

**المطلوب — أحد خيارين:**
- **(أ) الأبسط:** توسيع `POST /opinions/worker` بحقل `detailedReport: string?` بجانب `notes`.
- **(ب) الأدق:** مسار `PUT` مستقل لكل قسم، على نمط الأقسام التسعة الحالية.

---

## 🔴 طلب 5 — `GET /api/v1/cases/{id}/field-verification`

*(أولوية لأن ضرره يقع في الاستخدام اليومي العادي، لا حالة استثنائية.)*

**المشكلة:** `PUT /cases/{id}/field-verification` استبدال كامل (full replace) — منطقي وموثّق، لكن الاستبدال الكامل يفرض معرفة الحالة الحالية قبل استبدالها. **لا يوجد أي `GET` لهذا المورد**، ولا هو مضمّن في `GET /cases/{id}`.

**الأثر — يقع في كل حالة تُزار أكثر من مرة:**
```
الزيارة ١ → تحقّق من 12 حقلًا → PUT → الخادم يحفظ 12
الزيارة ٢ → الشاشة لا تعرف الـ12 السابقة → الأخصائي يضيف حقلًا ١٣
         → PUT يرسل حقلًا واحدًا (استبدال كامل) → الخادم يحتفظ بحقل واحد فقط
```
**محو صامت.** نجاح `200`، لا خطأ ولا تحذير. الأخصائي لن يعرف، والفريق يكتشفها من تقرير ناقص بعد أسابيع. **لا حل من طرف العميل ممكن** — لا يمكن لأي عميل استبدال مجموعة استبدالًا كاملًا دون معرفتها أولًا (جرّبنا: منع التعديل بعد أول حفظ يخالف طريقة العمل، وكاش محلي يختفي مع إعادة التثبيت أو تغيير الجهاز).

**المطلوب:**
```
GET /api/v1/cases/{caseId}/field-verification
```
Permission: `view_cases`. الاستجابة: مجموعة التحقق الحالية + `caseRowVersion`. `originalValue`/`isDifferent` للقراءة فقط.

**بديل مقبول تمامًا:** تضمين `fieldVerification` داخل `GET /cases/{id}` بدل مسار مستقل.

---

## 🔴 طلب 6 — `GET /api/v1/cases/{id}/field-visits`

**المشكلة:** `POST /field-visits` ينشئ، `PUT /field-visits/{id}` يعدّل. **لا يوجد `GET`.** فحصنا الوثيقة كاملة — `field-visits` ترد 13 مرة، كلها POST/PUT فقط، وغير مضمّنة في `GET /cases/{id}`.

**الأثر أ — فقدان سجلّ الزيارات:** أخصائي سجّل 6 زيارات على مدى شهر، كسر هاتفه أو أعاد تثبيت التطبيق أو انتقل لجهاز جديد. الزيارات موجودة عندكم سليمة، والتطبيق لا يملك وسيلة لجلبها. إما يعيد الإدخال، أو الأسوأ — يظن أنها لم تُسجَّل فيُكرّرها في السجل الرسمي.

**الأثر ب — تعطّل التعافي من الانقطاع:** الوثيقة نفسها توثّق (§14.2) أن `POST /field-visits` بلا `Idempotency-Key`، فإعادة إرسال بعد انقطاع تُنشئ زيارة مكررة. نقطة التعافي الطبيعية (اسأل الخادم عن زيارات الحالة وطابِق) معطّلة لعدم وجود `GET`. الكود مكتوب على افتراض وجوده:
```dart
// lib/features/field_visits/data/field_visits_api.dart:112
Future<List<Map<String, dynamic>>> listForCase(String caseId) =>
    _client.get('/cases/$caseId/field-visits', Parse.list);   // ← 404 اليوم
```

**العلاقة بطلب `Idempotency-Key` (رقم 8 أدناه):** الطلبان يحلّان نفس المشكلة من طرفين، وتنفيذ أحدهما يخفّف الإلحاح، لكن أثر (أ) — فقدان السجل عند تغيير الجهاز — لا يحلّه `Idempotency-Key` إطلاقًا.

**المطلوب:**
```
GET /api/v1/cases/{caseId}/field-visits?page=1&limit=20
```
Permission: `view_cases`. نفضّل رؤية **كل** زيارات الحالة المسندة إليه لا زياراته هو فقط — والقرار لكم. الاستجابة: نفس شكل استجابة `POST /field-visits` حرفيًا (§20.9)، **و`rowVersion` ضروري** — بدونه `PUT /field-visits/{id}` بعد إعادة التثبيت مستحيل.

**لماذا نتوقع أن يكون سهلًا:** الكيان والـmapping موجودان بالفعل ويُستخدَمان في استجابة `POST`. استعلام قراءة فوق بنية قائمة، لا منطق جديد.

---

## 🟠 طلب 10 — تعديل الزيارة الميدانية مقيَّد بمنشئها، لا بالأخصائي الحالي للحالة

*(اكتُشف 2026-09-19 أثناء بناء شاشة الزيارات الميدانية في التطبيق.)*

**المشكلة:** وثّقتم بوضوح (§20.10) أن `PUT /field-visits/{id}` يبحث عن الزيارة أولًا بمعرّفها، وإن لم تكن موجودة **أو** كان `SocialWorkerId` الخاص بها لا يساوي المستخدم الحالي، يرجع `404 NOT_FOUND` — بلا تمييز بين الحالتين (عن قصد، لمنع الكشف). المشكلة أن `SocialWorkerId` هنا هو **من أنشأ الزيارة أصلًا**، وليس الأخصائي المسندة له الحالة حاليًا.

هذا يتعارض مع الحالة التشغيلية الطبيعية للتطبيق: الحالة تتنقّل بين أخصائيين (إرجاع، إعادة إسناد)، والزيارات الميدانية جزء من سجل الحالة نفسها لا ملكية شخصية للأخصائي.

**السيناريو:**
```
الحالة مسندة لأحمد → أحمد يسجّل زيارة ١ (outcome فيه خطأ إملائي أو نتيجة ناقصة)
الحالة تُعاد إسنادها لسارة (إرجاع من المراجع، أو انتقال طبيعي في سير العمل)
سارة تفتح سجل الزيارات → ترى زيارة ١ (القراءة متاحة) لكن:
  PUT /field-visits/{زيارة-١-id} → 404 NOT_FOUND (رغم أن الحالة حاليًا مسندة لها فعليًا)
```

النتيجة العملية: سارة **لا تقدر تصحّح أو تكمّل** زيارة زميلها السابق — الخيار الوحيد المتاح لها هو إضافة زيارة جديدة منفصلة، فيتضاعف عدد الزيارات في السجل الرسمي لأسباب لا علاقة لها بعدد الزيارات الميدانية الفعلية، ويصعب على المراجع/المدير قراءة السجل لاحقًا.

**المطلوب:** توسيع شرط التفويض في `PUT /field-visits/{id}` (`FieldVisitAuthorization`) ليقبل **أيًّا من**:
- `SocialWorkerId` الخاص بالزيارة == المستخدم الحالي (السلوك الحالي، يبقى كما هو)، **أو**
- `Case.AssignedToUserId` == المستخدم الحالي (الأخصائي المسندة له الحالة حاليًا، بصرف النظر عمّن أنشأ الزيارة)

**ملاحظة على non-disclosure:** الشرط الحالي (404 موحّد لعدم الوجود/عدم الملكية) يبقى صحيحًا ومرغوبًا لو الفاحص ليس مُسندًا له الحالة أصلًا — التوسيع المطلوب فقط يضيف حالة "أنت الأخصائي الحالي للحالة" كشرط بديل كافٍ، لا يُضعف الفحص لغير المُسنَد إليهم.

**العلاقة بالطلب 6:** طلب 6 (`GET /field-visits`) ضروري بشكل مستقل لعرض سجل الزيارات كاملًا لأي أخصائي حالي — هذا الطلب (10) عن التعديل تحديدًا بعد أن تُعرَض.

---

## 🟡 طلب 7 — `Idempotency-Key` على `POST /cases/{id}/field-visits`

**المشكلة:** موثّق صراحة في العقد (§14.2) أن هذا المسار بلا دعم `Idempotency-Key`، فإعادة إرسال بعد فقد الرد تُنشئ زيارة ثانية. أخطر endpoint بالنسبة لنا: يُستدعى من الميدان (أسوأ شبكة)، من طابور أوفلاين قد يُعاد تشغيله، والتكرار يعني بيانات فاسدة في سجل رسمي لا مجرد إزعاج.

**المطلوب:** دعم `Idempotency-Key` بنفس آلية المسارات التسعة الحالية (تخزين الاستجابة 24 ساعة بمفتاح `(key, userId)`). نفضّل اختياريًا بالبداية، إلزاميًا لاحقًا.

**حلّنا المؤقت:** `dedupId` محلي دائم + علامة `inFlight` + مطابقة عند الإقلاع — تخفيف لا حل. لا عميل يضمن exactly-once بدون دعم من الخادم.

---

## 🟢 طلب 8 — توحيد سلوك الـ pagination

**المشكلة:** `page=0` يُنتج `422 VALIDATION_ERROR` في `work-queue`/`notifications`، ويُقَصّ بصمت لـ1 في `search/cases`/`attachments`. موثّقة كـ"genuine gotcha" في الوثيقة نفسها (§21).

**المطلوب:** توحيد على **القصّ الصامت** في كل المسارات — الأنسب لعميل موبايل قد يرسل صفحة محسوبة من كاش قديم.

---

## 🟢 طلب 9 — إرجاع المصفوفات كمصفوفات لا كنصوص JSON

**المشكلة:** `selectedLivestock` و`mainClassifications` تُرسَلان كمصفوفة في الطلب وتعودان كنص (`selectedLivestockJson`, `mainClassificationsJson`) في الاستجابة. موثّقة كـ"gotcha worth flagging" في الوثيقة نفسها.

**المطلوب:** إرجاعهما كمصفوفات بنفس اسم حقل الطلب. لو كسّار للويب: حقل جديد بجانب القديم.

---

## ❓ أسئلة مرتبطة (لا تحتاج تعديل كود)

1. **`GET /cases` الفلاتر:** الفلاتر الوحيدة `status`/`bookmarked` — نيّة لإضافة `assignedTo`، أم نعتمد نهائيًا على `/dashboard/work-queue`؟
2. **`appliances[].applianceKey`:** قائمة مفاتيح معتمدة؟ نحتاجها لعرض أسماء عربية صحيحة أوفلاين.
3. **`assessedNeeds[].source`/`.status`:** قيم enum معتمدة نلتزم بها؟
4. **حدّ المرفقات:** حدّ عدد/حجم إجمالي للحالة الواحدة؟ الزيارة قد تحمل 10–15 صورة.
5. **صلاحية `uploadUrl`:** مدة `uploadUrlExpiresAtUtc` بالضبط؟ نحتاجها لحساب إعادة `/init` لصورة أوفلاين.
6. **الإشعارات و`caseId`:** مضمون الوجود في كل إشعار متعلق بحالة؟ نبني عليه فتح الحالة من الإشعار مباشرة.
7. **`photoAttachmentIds` بعد الحذف:** لو حُذف مرفق مرتبط بزيارة، يُزال تلقائيًا من `photoAttachmentIds` أم يبقى معرّفًا معلّقًا؟
8. **حدّ الزيارات:** حدّ لعدد الزيارات للحالة الواحدة؟ يحدّد ضرورة الترقيم في طلب 6.

---

## الخلاصة

الطلبات **1، 2، 3، 4، 5، 6** توقف شاشات فعليًا أو تُفقد بيانات — نقدّر أي تحديث عن موقفها ولو أولي. الطلب **10** أقل إلحاحًا (السيناريو يحتاج تحوّل إسناد + زيارة سابقة) لكنه سيصبح مشكلة يومية فور انتشار الاستخدام. نواصل العمل الآن على كل ما لا يتوقف عليكم (محرّك المزامنة، مسارات الكتابة، ربط الشاشات، شاشة التعارض، شاشة الزيارات الميدانية بالقيد الحالي).

شكرًا — وثيقتكم، وتحديدًا توثيقكم الصريح للثغرات مثل §14.2 بدل إخفائها، هي ما مكّننا من اكتشاف أغلب هذه الفجوات على المكتب بدل الاصطدام بها في الميدان.
