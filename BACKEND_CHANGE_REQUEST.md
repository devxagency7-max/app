# طلبات تعديل على الـ API — من فريق تطبيق الموبايل (Flutter)

**التاريخ:** 2026-09-18
**المرجع:** `FLUTTER_API_DOCUMENTATION.md`
**السياق:** التطبيق سيعمل **Offline-First** — الأخصائي يملأ الحالة كاملة ويصوّر ويسجّل الزيارة بدون اتصال، وكل شيء يُرفَع تلقائيًا عند عودة الشبكة. بعض الطلبات أدناه ناتجة مباشرة عن هذا المتطلب.

الطلبات مرتّبة حسب الأولوية. الطلبات 1–3 **تُعطّل شاشات كاملة** في التطبيق.

---

## 🔴 طلب 1 — تمييز مصدر الإرجاع (مراجع / مدير)

### المشكلة

الحالة المرتجعة من **المراجع** والحالة المرتجعة من **المدير** تصلان للتطبيق بنفس القيمة تمامًا: `status: "returned_to_worker"`.

- `POST /cases/{id}/return-to-worker` ← المراجع
- `POST /cases/{id}/return-for-completion` ← المدير

التطبيق **لا يملك أي وسيلة للتفريق بينهما**.

### لماذا هذا مهم

شاشة الأخصائي تعرض حاليًا حالتين منفصلتين: "مرتجعة من المراجع" و"مرتجعة من المدير". الفرق تشغيلي حقيقي:

- الإرجاع من **المراجع** غالبًا نقص بيانات ← الأخصائي يستكمل ويعيد الإرسال
- الإرجاع من **المدير** غالبًا مراجعة قرار أو قيمة دعم ← يحتاج تنسيقًا مع رئيس الوحدة

عرضهما بنفس الشكل يُفقد الأخصائي سياقًا يحدّد أولويته وطريقة تعامله.

### المطلوب

إضافة كائن `returnInfo` إلى استجابة `GET /api/v1/cases/{id}`:

```json
{
  "id": "5b1e2a3c-...",
  "status": "returned_to_worker",
  "returnInfo": {
    "returnedBy": "reviewer",
    "returnedByName": "أ. حسام الدين",
    "reason": "برجاء استكمال بيانات السكن",
    "returnedAtUtc": "2026-09-18T10:15:00Z"
  }
}
```

**المواصفات:**

| الحقل | النوع | ملاحظات |
|---|---|---|
| `returnInfo` | object? | `null` عندما لا تكون الحالة مرتجعة |
| `returnedBy` | string | `"reviewer"` أو `"manager"` — قيم wire ثابتة |
| `returnedByName` | string? | الاسم المعروض لمن أرجع الحالة |
| `reason` | string | سبب الإرجاع (موجود لديكم بالفعل في `opinion.ReturnReason`) |
| `returnedAtUtc` | datetime | وقت آخر إرجاع |

يعكس **آخر** عملية إرجاع فقط.

### البديل لو التنفيذ صعب

إضافة `returnedBy` فقط (`"reviewer"` / `"manager"`) يحلّ 80% من المشكلة. باقي الحقول متاح من مصادر أخرى.

### إضافة مطلوبة على القائمة

نفس المعلومة مطلوبة في عناصر `GET /dashboard/work-queue` و `GET /cases` — الأخصائي يحتاج رؤية الفرق **في قائمة الحالات** قبل فتح الحالة. يكفي حقل مسطّح واحد:

```json
"returnedBy": "reviewer"
```

---

## 🔴 طلب 2 — توضيح ناتج `return-for-completion` على حقل `status`

### المشكلة

الوثيقة توضّح بجلاء أن `POST /cases/{id}/return-to-worker` ينقل الحالة إلى `returned_to_worker`.

لكن **ناتج `POST /cases/{id}/return-for-completion` على حقل `status` غير موثّق بنفس الوضوح**.

### المطلوب

تأكيد مكتوب لواحد من الاحتمالين:

- **(أ)** ينقل الحالة إلى `returned_to_worker` (نفس المراجع)
- **(ب)** ينقلها إلى حالة أخرى — ما هي بالضبط؟

وإضافة السطر المقابل لجدول الانتقالات في الوثيقة.

### لماذا

التطبيق يحتاج معرفة الحالات التي تفتح نافذة الزيارة الميدانية وإعادة إرسال الرأي. `FieldVisitAuthorization.AllowedStatuses` تشمل `ReturnedToWorker` — فإن كان `return-for-completion` يُنتج حالة مختلفة، **يصبح الأخصائي عاجزًا عن العمل على حالة أرجعها المدير**.

---

## 🔴 طلب 3 — إضافة 8 حقول مفقودة للمستفيد

### المشكلة

هذه الحقول **موجودة في النموذج الورقي المعتمد** ويملؤها الأخصائي في الميدان، لكنها **غير موجودة** في `PUT /api/v1/cases/{id}/beneficiary` ولا في `GET /api/v1/cases/{id}`.

### المطلوب

| الحقل | النوع | الحد | الوصف |
|---|---|---|---|
| `email` | string? | 255 | البريد الإلكتروني |
| `street` | string? | 255 | الشارع |
| `buildingNumber` | string? | 50 | رقم العقار |
| `floor` | string? | 50 | الدور |
| `apartmentNumber` | string? | 50 | رقم الشقة |
| `landmark` | string? | 255 | علامة مميزة |
| `area` | string? | 255 | المنطقة |
| `employer` | string? | 255 | جهة العمل |

**كلها اختيارية (`nullable`)** — لا تكسر أي عميل قائم ولا تؤثر على التحقق الحالي.

**السبب:** العنوان التفصيلي ضروري للوصول للمستفيد في القرى، حيث لا تكفي "المركز + القرية" للعثور على المنزل. الأخصائي يكتبها ورقيًا اليوم لأن التطبيق لا يحفظها.

### ملاحظة على حساب نسبة الإكمال

هذه الحقول اختيارية، فنرجو **ألّا تدخل في حساب `CaseCompletionCalculator`** حتى لا ترتفع متطلبات الوصول لـ 100% على الحالات القائمة.

---

## 🟡 طلب 4 — `Idempotency-Key` على `POST /cases/{id}/field-visits`

### المشكلة

الوثيقة تنصّ صراحة (§14.2):

> **`POST /cases/{caseId}/field-visits`** — no `Idempotency-Key` support... a retried `POST` here creates a second visit row server-side. **This is a real gap in the backend contract for this specific endpoint.**

هذا **أخطر endpoint في التطبيق** بالنسبة لنا، لأنه:

1. يُستدعى من **الميدان** حيث الشبكة أسوأ ما تكون
2. يُستدعى من **طابور أوفلاين** قد يُعاد تشغيله بعد انقطاع
3. تكرار الزيارة يعني **بيانات فاسدة** في سجل رسمي، لا مجرد إزعاج

السيناريو الواقعي: الأخصائي يضغط "حفظ الزيارة"، الطلب يصل للخادم، الرد يضيع بسبب انقطاع الشبكة. التطبيق لا يعرف هل نجح أم لا. أي قرار يتخذه خاطئ: إعادة الإرسال → زيارة مكررة؛ عدم الإرسال → زيارة ضائعة.

### المطلوب

دعم `Idempotency-Key` على هذا المسار، **بنفس الآلية المستخدمة في المسارات التسع** (تخزين الاستجابة 24 ساعة بمفتاح `(key, userId)`).

نفضّل أن يكون **اختياريًا في البداية** حفاظًا على التوافق، ثم إلزاميًا لاحقًا.

### حلّنا المؤقت

بنينا حلًا محليًا (`dedupId` دائم + تعليم `inFlight` + مطابقة عند إقلاع التطبيق)، لكنه **يبقى تخفيفًا لا حلًا**: لا يمكن لأي عميل ضمان exactly-once بدون دعم من الخادم. حالة "مات التطبيق أثناء الإرسال" تبقى غير محسومة يقينًا.

---

## 🟢 طلب 5 — توحيد سلوك الـ pagination

### المشكلة

نفس المدخلات تُنتج سلوكين مختلفين:

| Endpoint | `page=0` |
|---|---|
| `GET /dashboard/work-queue` | **422 VALIDATION_ERROR** |
| `GET /notifications` | **422 VALIDATION_ERROR** |
| `GET /search/cases` | يُقَصّ بصمت إلى 1 |
| `GET /cases/{id}/attachments` | يُقَصّ بصمت إلى 1 |

الوثيقة نفسها تصف هذا بـ "genuine gotcha" (§21).

### المطلوب

توحيد السلوك على **القصّ الصامت** في كل المسارات (الأكثر تسامحًا والأنسب لعميل موبايل قد يرسل صفحة محسوبة من كاش قديم).

**الأولوية: منخفضة** — تعاملنا معه، لكنه مصدر أخطاء دائم لأي عميل جديد.

---

## 🟢 طلب 6 — إرجاع المصفوفات كمصفوفات لا كنصوص JSON

### المشكلة

حقلان يُرسَلان **مصفوفة** في الطلب ويعودان **نصًّا** في الاستجابة:

| القسم | في الطلب | في الاستجابة |
|---|---|---|
| `PUT /cases/{id}/agriculture` | `"selectedLivestock": ["cows","other"]` | `"selectedLivestockJson": "[\"cows\",\"other\"]"` |
| `PUT /cases/{id}/classification` | `"mainClassifications": ["فقر","إعاقة"]` | `"mainClassificationsJson": "[\"فقر\",\"إعاقة\"]"` |

الوثيقة توثّقه كـ "gotcha worth flagging to frontend devs explicitly".

### المطلوب

إرجاعهما كمصفوفات بنفس اسم حقل الطلب:

```json
"selectedLivestock": ["cows", "other"],
"mainClassifications": ["فقر", "إعاقة"]
```

**لو كان تغييرًا كاسرًا للويب:** إضافة الحقل الجديد **بجانب** القديم، ونستخدم نحن الجديد.

**الأولوية: منخفضة** — نعالجه بـ `jsonDecode`، لكنه يخالف باقي العقد المتّسق.

---

## ❓ أسئلة توضيحية (لا تحتاج تعديل كود)

1. **`GET /cases` الفلاتر:** الوثيقة تؤكد أن الفلاتر الوحيدة هي `status` و `bookmarked` — لا `assignedTo`. هل هناك نيّة لإضافة فلاتر أخرى، أم نعتمد نهائيًا على `/dashboard/work-queue` لحالات الأخصائي؟

2. **`appliances[].applianceKey`:** هل توجد قائمة معتمدة بالمفاتيح (`fridge`, `tv`, ...)؟ الوثيقة تصفها "free-form". نحتاج قائمة ثابتة لعرض أسماء عربية صحيحة **أوفلاين**.

3. **`assessedNeeds[].source` و `.status`:** الوثيقة تذكر عدم وجود enum ثابت. هل هناك قيم معتمدة نلتزم بها؟

4. **حدّ المرفقات:** 10 ميجا للملف الواحد — هل هناك حدّ لعدد المرفقات للحالة الواحدة أو حدّ تخزين إجمالي؟ الزيارة الميدانية قد تحمل 10–15 صورة.

5. **صلاحية `uploadUrl`:** كم مدة `uploadUrlExpiresAtUtc` بالضبط؟ نحتاجها لحساب متى نُعيد `/init` لصورة التُقطت أوفلاين.

6. **الإشعارات و `caseId`:** هل `caseId` مضمون الوجود في كل إشعار متعلق بحالة؟ نبني عليه فتح الحالة مباشرة من الإشعار.

---

## ملخّص الأولويات

| # | الطلب | الأولوية | يُعطّل |
|---|---|:---:|---|
| 1 | `returnInfo` لتمييز الإرجاع | 🔴 | شاشة الإرجاع |
| 2 | توضيح `return-for-completion` | 🔴 | منطق سير العمل |
| 3 | 8 حقول المستفيد | 🔴 | تبويب البيانات الأساسية |
| 4 | `Idempotency-Key` للزيارات | 🟡 | لا يُعطّل — يقلّل خطر فساد بيانات |
| 5 | توحيد pagination | 🟢 | لا |
| 6 | المصفوفات في الاستجابة | 🟢 | لا |

**الطلبات 1–3 توقف شاشات فعليًا.** نبدأ الآن بالمراحل المستقلّة (نواة الشبكة، قاعدة البيانات المحلية، المصادقة، القراءة، الإشعارات) ونصل إليها فور ردّكم.

نقدّر جودة الوثيقة — توثيق الثغرات صراحةً (مثل §14.2) وفّر علينا وقتًا كبيرًا.
